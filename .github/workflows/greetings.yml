name: Greetings

on: [pull_request_target, issues]

jobs:
  greeting:
    runs-on: ubuntu-latest
    permissions:
      issues: write
      pull-requests: write
    steps:
    - uses: actions/first-interaction@v1
      with:
        repo-token: ${{ secrets.GITHUB_TOKEN }}
        issue-message: "Opa! Ago deu errado, verifique ante de prosseguir."
        pr-message: "Esse é seu primeiro pull request aqui! Jaja verifico."
